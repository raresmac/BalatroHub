@inherits LayoutComponentBase

<div class="balatro-bg"></div>
<div class="crt-overlay"></div>

<main>
    <article class="content px-4">
        @Body
    </article>
</main>

<div class="poker-nav">
    <NavLink href="" Match="NavLinkMatch.All" class="nav-item">HOME</NavLink>
    <NavLink href="decks" class="nav-item">DECKS</NavLink>
    <NavLink href="challenges" class="nav-item">CHALLENGES</NavLink>
    <NavLink href="seeds" class="nav-item">SEEDS</NavLink>
</div>

<style>
    .poker-nav {
        position: fixed;
        bottom: 15px;
        left: 50%;
        transform: translateX(-50%);
        display: flex;
        flex-wrap: wrap; /* Nav wraps on tiny screens */
        justify-content: center;
        gap: 10px;
        z-index: 1000;
        background: rgba(0,0,0,0.9);
        padding: 10px 15px;
        border-radius: 12px;
        border: 1px solid #444;
        width: auto;
        max-width: 95vw;
    }

    .nav-item {
        color: white;
        text-decoration: none;
        font-weight: bold;
        font-size: 0.9rem;
        padding: 5px 10px;
        white-space: nowrap;
    }

        .nav-item:hover, .nav-item.active {
            color: var(--balatro-gold);
        }
</style>